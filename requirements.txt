# Core runtime: Confluence ingest, chunking, vector store, retrieval, API.
# The embedding model itself lives in requirements-embeddings.txt.

pyyaml>=6.0
pydantic>=2.7
httpx>=0.27
tenacity>=8.3
beautifulsoup4>=4.12
typer>=0.12
rich>=13.7
structlog>=24.1
fastapi>=0.111
uvicorn[standard]>=0.30
chromadb>=0.5.0
numpy>=1.26

# Optional production vector store. Safe to omit while running on Chroma.
psycopg[binary]>=3.1
